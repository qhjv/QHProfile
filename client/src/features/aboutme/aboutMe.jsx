import React from 'react';
import PropTypes from 'prop-types';

AboutMe.propTypes = {
    
};

function AboutMe(props) {
    return (
        <div>
            about
        </div>
    );
}

export default AboutMe;